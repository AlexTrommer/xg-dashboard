[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python data.py && uvicorn api:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/status"
healthcheckTimeout = 1800
restartPolicyType = "ON_FAILURE"
